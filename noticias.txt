[-- htmlex 0.4.4 release --]

* Arreglado un error cuando se definen macros dentro de una función.

[-- htmlex 0.4.3 release --]

* Corregida la explicación del argumento -a.
* Arreglado un error al llamar macros funcionales donde uno de sus
  argumentos utilizase <!include>.

[-- htmlex 0.4.2 release --]

* Nueva opción -E para cambiar la extensión por defecto .html
  utilizada por -c.
* Se cambió la forma en que se procesan los argumentos, le recomiendo
  ver la sección "EJECUCIÓN" del archivo "leame.txt".
* Arreglados un par de errores con las dependencias y las macros
  funcionales.
* Nueva conducta por defecto para los comentarios: Ahora son
  preservados, y un nuevo parámetro (-k) fue agregado para
  eliminarlos.
* Optimizada la generación de dependencias: Ya que desde ahora las
  etiquetas dentro de una expresión <!if...> falsa, no son procesadas.

[-- htmlex 0.4 release --]

* Nuevo script htmlex-filter.pl para "limpiar" la salida de htmlex. 
* Nueva etiqueta "<!function nombre {argumentos}>cuerpo<!end>".
  Para hacer macros como en C/C++.
* Arreglado el funcionamiento de los argumentos con comillas (ahora
  acepta \n \t \" etc.).
* Arreglados algunos errores menores.

[-- htmlex 0.3.2 release --]

* Algunas notas sobre macros en leame.txt.
* Arreglada la etiqueta <!elif...> y el comportamiento general del <!if>.
* Arreglado un error de memoria.
* Agregado un modo verboso.

[-- htmlex 0.3.1 release --]

Por Robert J Ohannessian:
  * El archivo de texto `readme.txt' está en un inglés más claro.
  * <!file-size> presenta el tamaño del fichero de una mejor forma.
  * El programa compila en MinGW sin problemas.

[-- htmlex 0.3 release --]

* Completada la documentación en inglés.
* Arreglados varios errores.
* La etiqueta <!file-size> genera dependencias.
* Nueva etiqueta <!dep>.

[-- htmlex 0.2 pre-release --]

* Mayores facilidades para las comparaciones <!if>.
* Nuevo funcionamiento para varias etiquetas, para más información
  ver `leame.txt'.
* Nuevos nombres de etiquetas:
  <!uc>, <!ucfirst>                   -> <!toupper>
  <!lc>, <!lcfirst>                   -> <!tolower>
  <!htmlex-version>                   -> <!version>
  <!include-path archivo> y <!exists> -> <!find archivo>
* Nuevo código en C, y programa ejecutable.
* Eliminado el formato GNU.

[-- htmlex 0.1.3 beta --]

* Nueva etiqueta <!file-size>.

[-- htmlex 0.1.2 beta --]

* Nueva etiqueta <!clean>.
* Mejoras en la utilidad htmlex-site.
* Nueva etiqueta <!exists> para averiguar si un fichero existe o no.
* Mayor velocidad de procesamiento.
* <!htmlex-version> para imprimir la versión de htmlex.
* Una pobre (y mala) documentación en inglés.
* Optimizado htmlex-site para crear el "esqueleto" de un nuevo sitio.

[-- htmlex 0.1.1 beta --]

* Etiquetas `<!if...>' para controlar bloques de condiciones:
    <!if>
    <!elif>
    <!else>
    <!fi>
  Se soportan anidamientos (teóricamente) infinitos.

[-- htmlex 0.1.0 pre-alpha --]

* Los ficheros HTML (.html) convencionales son totalmente compatible con el
  formato utilizado por htmlex (.htex).
* Etiquetas extras con formato similar al HTML.
* Etiqueta `<!macro...>' para crear macros que serán reemplazadas por su valor
  cuando se encuentre el nombre exacto de la misma.
* Etiqueta `<!include...>' para incluir todo un ficheros dentro de otro.
* Generación de dependencias apartir de los `<!include...>' totalmente
  compatibles con los Makefiles.
* Soporte para pasarle argumentos al fichero principal de entrada, y a los
  ficheros incluidos detro del mismo.
* Etiqueta `<!exec...>' para ejecutar programas externos y colocar la salida en
  el fichero resultado.
* Funciones para controlar texto:
  <!dir palabra>
  <!notdir palabra>
  <!suffix palabra>
  <!basename palabra>
    (Todas con el mismo funcionamiento que en los Makefiles).
  <!chop palabra>
  <!shift palabra>
  <!lc palabra>
  <!lcfirst palabra>
  <!uc palabra>
  <!ucfirst palabra>
    (Todas con el mismo funcionamiento que en Perl).
